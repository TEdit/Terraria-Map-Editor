<UserControl x:Class="TEditWPF.Views.WorldImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:TEditWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <views:WorldImageViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#666666">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid x:Name="WorldImage" Background="#FF333333" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  />
            <Grid x:Name="Overlay" Background="#30FF0000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  
                  MouseMove="ViewportMouseMove" 
                  MouseDown="ViewportMouseDown"
                  MouseWheel="ViewportMouseWheel" 
                  MouseUp="ViewportMouseUp" 
                  MouseEnter="ViewportMouseEnter" 
                  MouseLeave="ViewportMouseLeave" />
            <ScrollBar Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Grid.Row="1" ViewportSize="500" Maximum="1000" LargeChange="10" Value="{Binding Path=ScrollPositionHorizontal, Mode=TwoWay}" />
            <ScrollBar Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Stretch" Grid.Column="1" ViewportSize="500" Maximum="1000" LargeChange="10" Value="{Binding Path=ScrollPositionVertical, Mode=TwoWay}" />
        </Grid>
        <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Center" Margin="1">
            <Border BorderBrush="Black" BorderThickness="1" Margin="1" Padding="1" >
                <TextBlock Text="{Binding Path=MouseOverTile}" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

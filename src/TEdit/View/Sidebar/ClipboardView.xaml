<UserControl x:Class="TEdit.View.ClipboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:p="clr-namespace:TEdit.Properties"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="clipboardItemView">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding Path=Buffer.Name}" HorizontalAlignment="Center" Margin="1" Foreground="{DynamicResource TextBrush}"/>
                <Image Source="{Binding Path=Preview}" HorizontalAlignment="Stretch" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor" MaxHeight="100" />
                <UniformGrid Columns="3">
          <Button ToolTip="{x:Static p:Language.tool_clipboard_paste}" HorizontalAlignment="Stretch" Margin="1" Padding="3" Command="{Binding DataContext.ClipboardSetActiveCommand, Mode=Default, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}">
            <ui:SymbolIcon Symbol="ClipboardPaste20" FontSize="20" />
          </Button>
          <Button ToolTip="{x:Static p:Language.tool_clipboard_export}" HorizontalAlignment="Stretch" Margin="1" Padding="3" Command="{Binding DataContext.ExportSchematicCommand, Mode=Default, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}">
            <ui:SymbolIcon Symbol="Save20" FontSize="20" />
          </Button>
          <Button ToolTip="{x:Static p:Language.tool_clipboard_remove}" HorizontalAlignment="Stretch" Margin="1" Padding="3" Command="{Binding DataContext.RemoveSchematicCommand, Mode=Default, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}">
            <ui:SymbolIcon Symbol="Delete20" FontSize="20" />
          </Button>
          <Button ToolTip="{x:Static p:Language.tool_clipboard_rotate}" HorizontalAlignment="Stretch" Margin="1" Padding="3" Command="{Binding DataContext.ClipboardRotateCommand, Mode=Default, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}" >
            <ui:SymbolIcon Symbol="RotateRight20" FontSize="20" />
          </Button>
          <Button ToolTip="{x:Static p:Language.tool_clipboard_flipx}" HorizontalAlignment="Stretch" Margin="1" Padding="3" Command="{Binding DataContext.ClipboardFlipXCommand, Mode=Default, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}" >
            <ui:SymbolIcon Symbol="FlipHorizontal20" FontSize="20" />
          </Button>
          <Button ToolTip="{x:Static p:Language.tool_clipboard_flipy}" HorizontalAlignment="Stretch" Margin="1" Padding="3" Command="{Binding DataContext.ClipboardFlipYCommand, Mode=Default, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}" >
            <ui:SymbolIcon Symbol="FlipVertical20" FontSize="20" />
          </Button>
                </UniformGrid>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <DockPanel Background="{DynamicResource SidebarBackgroundBrush}" LastChildFill="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <!-- Title -->
        <TextBlock DockPanel.Dock="Top" Text="{x:Static p:Language.editor_tabs_clipboard}" Style="{DynamicResource SidebarPanelTitle}" />

        <StackPanel Orientation="Vertical" DockPanel.Dock="Top" HorizontalAlignment="Stretch">
            <!-- Toolbar -->
            <UniformGrid Columns="2" Style="{DynamicResource SidebarButtonRow}">
                <ui:Button Content="{x:Static p:Language.tool_clipboard_empty}" Margin="2" Padding="6,4" HorizontalAlignment="Stretch" Command="{Binding EmptyClipboardCommand}" />
                <ui:Button Content="{x:Static p:Language.tool_clipboard_importschematic}" Margin="2" Padding="6,4" HorizontalAlignment="Stretch" Command="{Binding ImportSchematicCommand}" CommandParameter="{StaticResource False}" />
            </UniformGrid>
            <!-- Instant paste toggle -->
            <ui:ToggleSwitch Margin="4,2" IsChecked="{Binding Clipboard.InstantPaste}" HorizontalAlignment="Center"
                             Content="{x:Static p:Language.tool_clipboard_instant_paste}" ToolTip="{x:Static p:Language.tool_clipboard_instant_paste_tooltip}" />
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
          <RowDefinition Height="*" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
                <CheckBox Grid.Row="0" Grid.Column="0" Content="{x:Static p:Language.tool_clipboard_ispasteempty}"  Margin="2" IsChecked="{Binding Clipboard.PasteEmpty}"/>
                <CheckBox Grid.Row="0" Grid.Column="1" Content="{x:Static p:Language.tool_clipboard_ispastetiles}"  Margin="2" IsChecked="{Binding Clipboard.PasteTiles}"/>
                <CheckBox Grid.Row="1" Grid.Column="0" Content="{x:Static p:Language.tool_clipboard_ispastewalls}"  Margin="2" IsChecked="{Binding Clipboard.PasteWalls}"/>
                <CheckBox Grid.Row="1" Grid.Column="1" Content="{x:Static p:Language.tool_clipboard_ispasteliquid}" Margin="2" IsChecked="{Binding Clipboard.PasteLiquids}"/>
                <CheckBox Grid.Row="2" Grid.Column="0" Content="{x:Static p:Language.tool_clipboard_ispastewire}" Margin="2" IsChecked="{Binding Clipboard.PasteWires}"/>
                <CheckBox Grid.Row="2" Grid.Column="1" Content="{x:Static p:Language.tool_clipboard_ispastesprite}" Margin="2" IsChecked="{Binding Clipboard.PasteSprites}"/>
                <CheckBox Grid.Row="3" Grid.Column="0" Content="{x:Static p:Language.tool_clipboard_replace}" Margin="2" IsChecked="{Binding Clipboard.PasteOverTiles}"/>
            </Grid>
        </StackPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl HorizontalAlignment="Stretch" ItemTemplate="{StaticResource clipboardItemView}" ItemsSource="{Binding Clipboard.LoadedBuffers}" />
        </ScrollViewer>
    </DockPanel>
</UserControl>

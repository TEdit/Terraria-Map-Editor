<UserControl x:Class="TEdit.View.Sidebar.SpecialTilesViews.MannequinEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:p="clr-namespace:TEdit.Properties"
    xmlns:converters="clr-namespace:TEdit.Converters"
    mc:Ignorable="d"
    d:DesignWidth="340">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SpecialTilesSharedResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="40" />
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- Header -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="4">
            <TextBlock Text="{x:Static p:Language.specialtile_label_mannequin}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" />
            <TextBlock Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold">
                <TextBlock.Text>
                    <MultiBinding StringFormat="({0}, {1})">
                        <Binding Path="SelectedTileEntity.PosX" />
                        <Binding Path="SelectedTileEntity.PosY" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
        <!-- Head -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static p:Language.specialtile_label_head}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Dye0.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="1" Grid.Column="2" ItemsSource="{Binding Source={StaticResource PrefixCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item0.Prefix, Mode=TwoWay}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="1" Grid.Column="3" ItemsSource="{Binding Source={StaticResource MannHeadCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item0.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key"
              ItemTemplate="{StaticResource RarityDictItemTemplate}"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <!-- Body -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="{x:Static p:Language.specialtile_label_body}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
        <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Dye1.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="2" Grid.Column="2" ItemsSource="{Binding Source={StaticResource PrefixCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item1.Prefix, Mode=TwoWay}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="2" Grid.Column="3" ItemsSource="{Binding Source={StaticResource MannBodyCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item1.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key"
              ItemTemplate="{StaticResource RarityDictItemTemplate}"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <!-- Legs -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static p:Language.specialtile_label_legs}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
        <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Dye2.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="3" Grid.Column="2" ItemsSource="{Binding Source={StaticResource PrefixCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item2.Prefix, Mode=TwoWay}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="3" Grid.Column="3" ItemsSource="{Binding Source={StaticResource MannLegsCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item2.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key"
              ItemTemplate="{StaticResource RarityDictItemTemplate}"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <!-- Acc 1 -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="{x:Static p:Language.specialtile_label_acc1}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
        <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Dye3.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="4" Grid.Column="2" ItemsSource="{Binding Source={StaticResource PrefixCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item3.Prefix, Mode=TwoWay}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="4" Grid.Column="3" ItemsSource="{Binding Source={StaticResource MannAccessoryCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item3.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key"
              ItemTemplate="{StaticResource RarityDictItemTemplate}"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <!-- Acc 2 -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="{x:Static p:Language.specialtile_label_acc2}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
        <ComboBox Grid.Row="5" Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Dye4.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="5" Grid.Column="2" ItemsSource="{Binding Source={StaticResource PrefixCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item4.Prefix, Mode=TwoWay}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="5" Grid.Column="3" ItemsSource="{Binding Source={StaticResource MannAccessoryCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item4.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key"
              ItemTemplate="{StaticResource RarityDictItemTemplate}"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <!-- Acc 3 -->
        <TextBlock Grid.Row="6" Grid.Column="0" Text="{x:Static p:Language.specialtile_label_acc3}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
        <ComboBox Grid.Row="6" Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Dye5.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="6" Grid.Column="2" ItemsSource="{Binding Source={StaticResource PrefixCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item5.Prefix, Mode=TwoWay}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="6" Grid.Column="3" ItemsSource="{Binding Source={StaticResource MannAccessoryCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item5.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key"
              ItemTemplate="{StaticResource RarityDictItemTemplate}"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <!-- Acc 4 -->
        <TextBlock Grid.Row="7" Grid.Column="0" Text="{x:Static p:Language.specialtile_label_acc4}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
        <ComboBox Grid.Row="7" Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Dye6.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="7" Grid.Column="2" ItemsSource="{Binding Source={StaticResource PrefixCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item6.Prefix, Mode=TwoWay}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="7" Grid.Column="3" ItemsSource="{Binding Source={StaticResource MannAccessoryCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item6.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key"
              ItemTemplate="{StaticResource RarityDictItemTemplate}"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <!-- Acc 5 -->
        <TextBlock Grid.Row="8" Grid.Column="0" Text="{x:Static p:Language.specialtile_label_acc5}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
        <ComboBox Grid.Row="8" Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Dye7.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="8" Grid.Column="2" ItemsSource="{Binding Source={StaticResource PrefixCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item7.Prefix, Mode=TwoWay}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="8" Grid.Column="3" ItemsSource="{Binding Source={StaticResource MannAccessoryCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Item7.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key"
              ItemTemplate="{StaticResource RarityDictItemTemplate}"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <!-- Weapon -->
        <TextBlock Grid.Row="9" Grid.Column="0" Text="{x:Static p:Language.specialtile_label_weapon}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
        <ComboBox Grid.Row="9" Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
              SelectedValue="{Binding SelectedTileEntity.WeaponDye.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="9" Grid.Column="2" ItemsSource="{Binding Source={StaticResource PrefixCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Weapon.Prefix, Mode=TwoWay}"
              IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <ComboBox Grid.Row="9" Grid.Column="3" ItemsSource="{Binding Source={StaticResource RackCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Weapon.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key"
              ItemTemplate="{StaticResource RarityDictItemTemplate}"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <!-- Mount -->
        <TextBlock Grid.Row="10" Grid.Column="0" Text="{x:Static p:Language.specialtile_label_mount}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
        <ComboBox Grid.Row="10" Grid.Column="3" ItemsSource="{Binding Source={StaticResource MountCollection}}"
              SelectedValue="{Binding SelectedTileEntity.Mount.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
              IsEditable="true" SelectedValuePath="Key"
              ItemTemplate="{StaticResource RarityDictItemTemplate}"
              IsSynchronizedWithCurrentItem="False"
              Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
        <!-- Buttons -->
        <UniformGrid Columns="2" Grid.Row="11" Grid.ColumnSpan="4">
            <Button Margin="2" Content="{x:Static p:Language.specialtile_button_cancel}" Command="{Binding SaveTileEntityCommand}"
                CommandParameter="{StaticResource False}" />
            <Button Margin="2" Content="{x:Static p:Language.specialtile_button_save}" Command="{Binding SaveTileEntityCommand}"
                CommandParameter="{StaticResource True}" />
        </UniformGrid>
    </Grid>
</UserControl>

<UserControl x:Class="TEdit.View.Sidebar.SpecialTilesViews.HatRackEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:p="clr-namespace:TEdit.Properties"
    xmlns:controls="clr-namespace:TEdit.View.Sidebar.Controls"
    mc:Ignorable="d"
    d:DesignWidth="340">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SpecialTilesSharedResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="{x:Static p:Language.specialtile_label_mannequinclothing}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" />
            <TextBlock Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" DockPanel.Dock="Top">
                <TextBlock.Text>
                    <MultiBinding StringFormat="({0}, {1})">
                        <Binding Path="SelectedTileEntity.PosX" />
                        <Binding Path="SelectedTileEntity.PosY" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static p:Language.specialtile_label_head}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
                  SelectedValue="{Binding SelectedTileEntity.Dye0.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
                  IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
                  IsSynchronizedWithCurrentItem="False"
                  Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
            <controls:ItemEditorControl Grid.Column="2"
                ItemId="{Binding SelectedTileEntity.Item0.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
                Prefix="{Binding SelectedTileEntity.Item0.Prefix, Mode=TwoWay}"
                ItemsSource="{Binding Source={StaticResource MannHeadCollection}}"
                ItemsSourceType="DictionaryKvp"
                PrefixSource="{Binding Source={StaticResource PrefixCollection}}"
                ShowStackSize="False" ShowActionButtons="False" />
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static p:Language.specialtile_label_head}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" VerticalAlignment="Center" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding Source={StaticResource DyeCollection}}"
                  SelectedValue="{Binding SelectedTileEntity.Dye1.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
                  IsEditable="true" SelectedValuePath="Key" DisplayMemberPath="Value"
                  IsSynchronizedWithCurrentItem="False"
                  Margin="1,0" ItemsPanel="{StaticResource Template_ComboPanel}" />
            <controls:ItemEditorControl Grid.Column="2"
                ItemId="{Binding SelectedTileEntity.Item1.Id, Mode=TwoWay, Converter={StaticResource ShortIntConverter}}"
                Prefix="{Binding SelectedTileEntity.Item1.Prefix, Mode=TwoWay}"
                ItemsSource="{Binding Source={StaticResource MannHeadCollection}}"
                ItemsSourceType="DictionaryKvp"
                PrefixSource="{Binding Source={StaticResource PrefixCollection}}"
                ShowStackSize="False" ShowActionButtons="False" />
        </Grid>
        <UniformGrid Columns="2" Grid.Row="3">
            <Button Margin="2" Content="{x:Static p:Language.specialtile_button_cancel}" Command="{Binding SaveTileEntityCommand}"
                CommandParameter="{StaticResource False}" />
            <Button Margin="2" Content="{x:Static p:Language.specialtile_button_save}" Command="{Binding SaveTileEntityCommand}"
                CommandParameter="{StaticResource True}" />
        </UniformGrid>
    </Grid>
</UserControl>

<UserControl x:Class="TEdit.View.Sidebar.SpecialTilesViews.WeaponRackEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:p="clr-namespace:TEdit.Properties"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:TEdit.View.Sidebar.Controls"
    mc:Ignorable="d"
    d:DesignWidth="340">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SpecialTilesSharedResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <TextBlock Text="{x:Static p:Language.specialtile_label_weaponrack}" Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" />
                <TextBlock Foreground="{DynamicResource TextBrush}" Margin="2" FontWeight="Bold" DockPanel.Dock="Top">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="({0:N}, {1:N})">
                            <Binding Path="SelectedTileEntity.PosX" />
                            <Binding Path="SelectedTileEntity.PosY" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
            <CheckBox x:Name="ShowAllItemsCheckBox" HorizontalAlignment="Right" VerticalAlignment="Center"
                Content="{x:Static p:Language.specialtile_show_all_items}"
                Checked="OnShowAllItemsChanged" Unchecked="OnShowAllItemsChanged" />
        </DockPanel>
        <controls:ItemEditorControl x:Name="ItemEditor" Grid.Row="1"
            ItemId="{Binding SelectedTileEntity.NetId, Mode=TwoWay}"
            Prefix="{Binding SelectedTileEntity.Prefix, Mode=TwoWay}"
            IsModItem="{Binding SelectedTileEntity.IsModItem, Mode=OneWay}"
            ModItemDisplayName="{Binding SelectedTileEntity.ItemDisplayName, Mode=OneWay}"
            ItemsSource="{Binding Source={StaticResource RackCollection}}"
            ItemsSourceType="DictionaryKvp"
            PrefixSource="{Binding Source={StaticResource PrefixCollection}}"
            ShowStackSize="False"
            ShowActionButtons="False" />
        <UniformGrid Columns="2" Grid.Row="2">
            <ui:Button Margin="5" Content="{x:Static p:Language.specialtile_button_cancel}" Command="{Binding SaveTileEntityCommand}"
                HorizontalAlignment="Stretch" Appearance="Danger"
                CommandParameter="{StaticResource False}" />
            <ui:Button Margin="5" Content="{x:Static p:Language.specialtile_button_save}" Command="{Binding SaveTileEntityCommand}"
                HorizontalAlignment="Stretch" Appearance="Success"
                CommandParameter="{StaticResource True}" />
        </UniformGrid>
    </Grid>
</UserControl>

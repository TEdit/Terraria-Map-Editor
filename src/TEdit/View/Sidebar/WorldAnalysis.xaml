<UserControl x:Class="TEdit.View.WorldAnalysis"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:p="clr-namespace:TEdit.Properties"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel Background="{DynamicResource SidebarBackgroundBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <!-- Title -->
        <TextBlock DockPanel.Dock="Top" Text="{x:Static p:Language.tool_analyze_title}" Style="{DynamicResource SidebarPanelTitle}" />

        <!-- Toolbar -->
        <UniformGrid DockPanel.Dock="Top" Columns="2" Style="{DynamicResource SidebarButtonRow}">
            <ui:Button Content="{x:Static p:Language.tool_analyze_analyze_button}" Margin="2" Padding="6,4" HorizontalAlignment="Stretch" Command="{Binding AnalyzeWorldCommand}" />
            <ui:Button Content="{x:Static p:Language.tool_analyze_save_button}" Margin="2" Padding="6,4" HorizontalAlignment="Stretch" Command="{Binding AnalyzeWorldSaveCommand}" />
        </UniformGrid>

        <TextBox Text="{Binding WorldAnalysis}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MinHeight="150"
               HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible" AcceptsReturn="True" TextWrapping="Wrap"
			   Margin="2"  />


    </DockPanel>
</UserControl>

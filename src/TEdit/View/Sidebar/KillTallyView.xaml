<UserControl x:Class="TEdit.View.KillTallyView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:p="clr-namespace:TEdit.Properties"
      mc:Ignorable="d"
      d:DesignHeight="300" d:DesignWidth="300">

    <DockPanel Background="{DynamicResource SidebarBackgroundBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <!-- Title -->
        <TextBlock DockPanel.Dock="Top" Text="{x:Static p:Language.tool_bestiary_title}" Style="{DynamicResource SidebarPanelTitle}" />

        <!-- Toolbar -->
        <UniformGrid DockPanel.Dock="Top" Columns="3" Style="{DynamicResource SidebarButtonRow}">
            <ui:Button Content="{x:Static p:Language.tool_bestiary_scan}" Margin="2" Padding="6,4" HorizontalAlignment="Stretch" Command="{Binding LoadTallyCommand}" />
            <ui:Button Content="{x:Static p:Language.tool_bestiary_import}" Margin="2" Padding="6,4" HorizontalAlignment="Stretch" Command="{Binding ImportBestiaryCommand}" />
            <ui:Button Content="{x:Static p:Language.tool_bestiary_edit}" Margin="2" Padding="6,4" HorizontalAlignment="Stretch" Command="{Binding EditBestiaryCommand}" />
        </UniformGrid>

        <TextBox Text="{Binding TallyCount}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MinHeight="150"
               HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible" AcceptsReturn="True" TextWrapping="Wrap"
			   Margin="2"  />


    </DockPanel>
</UserControl>

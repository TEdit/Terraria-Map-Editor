<UserControl x:Class="TEdit.View.Sidebar.PlayerEditor.PlayerJourneyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:p="clr-namespace:TEdit.Properties"
    mc:Ignorable="d" d:DesignWidth="320">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="2">
            <!-- Creative Sacrifices -->
            <TextBlock Text="{x:Static p:Language.player_journey_research_sacrifices}" Foreground="{DynamicResource TextBrush}" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock Text="{Binding Player.CreativeSacrifices.Count, StringFormat='Total researched items: {0}'}"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" Margin="0,0,0,4"/>

            <ListBox ItemsSource="{Binding Player.CreativeSacrifices}" MinHeight="200" MaxHeight="400"
                     ScrollViewer.CanContentScroll="True"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Key}" Foreground="White" Margin="2" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Grid.Column="1" Text="{Binding Value}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       Margin="2" TextAlignment="Right"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>

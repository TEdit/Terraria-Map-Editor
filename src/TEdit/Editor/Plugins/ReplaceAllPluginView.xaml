<Window x:Class="TEdit.Editor.Plugins.ReplaceAllPluginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:TEdit.View" SizeToContent="WidthAndHeight"
        Title="更换所有物块" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Icon="/TEdit;component/Images/tedit.ico">
    <StackPanel Background="{StaticResource ControlBackgroundBrush}" >
        <TextBlock Text="选择墙面或块涂漆模式, 然后启用伪装." Margin="10 2" TextWrapping="Wrap" HorizontalAlignment="Left" VerticalAlignment="Stretch" Foreground="{StaticResource TextBrush}"/>
        <view:PaintModeView MaxHeight="90" HorizontalAlignment="Stretch" VerticalAlignment="Top" Loaded="PaintModeView_Loaded" />
        <Grid>
            <Button Margin="5" Content="取消" HorizontalAlignment="Left" Padding="20, 3" VerticalAlignment="Center" Click="CancelButtonClick" />
            <Button Margin="5" Content="更换" HorizontalAlignment="Right" Padding="20, 3" VerticalAlignment="Center" Click="ReplaceButtonClick" />
        </Grid>
    </StackPanel>
</Window>

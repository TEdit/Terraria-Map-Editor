<Window x:Class="TEdit.Editor.Plugins.ReplaceAllPluginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:TEdit.View" SizeToContent="WidthAndHeight"
        Title="ReplaceAllPlugin" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Icon="/TEdit;component/Images/tedit.ico">
    <StackPanel Background="{StaticResource ControlBackgroundBrush}" >
        <TextBlock Text="Select wall or tile paint mode, then enable masks." Margin="10 2" TextWrapping="Wrap" HorizontalAlignment="Left" VerticalAlignment="Stretch" Foreground="{StaticResource TextBrush}"/>
        <view:PaintModeView MaxHeight="90" HorizontalAlignment="Stretch" VerticalAlignment="Top" />
        <Grid>
            <Button Margin="5" Content="Cancel" HorizontalAlignment="Left" Padding="20, 3" VerticalAlignment="Center" Click="CancelButtonClick" />
            <Button Margin="5" Content="Replace" HorizontalAlignment="Right" Padding="20, 3" VerticalAlignment="Center" Click="ReplaceButtonClick" />
        </Grid>
    </StackPanel>
</Window>

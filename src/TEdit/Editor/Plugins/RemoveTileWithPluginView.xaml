<Window x:Class="TEdit.Editor.Plugins.RemoveTileWithPluginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:TEdit.View" SizeToContent="WidthAndHeight"
        Title="移除方块插件" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Icon="/TEdit;component/Images/tedit.ico" Height="142.905">
    <StackPanel Background="{StaticResource ControlBackgroundBrush}" >
        <TextBlock Text="输入你想要移除的方块或物品的名称." Margin="10 2" TextWrapping="Wrap" HorizontalAlignment="Left" VerticalAlignment="Stretch" Foreground="{StaticResource TextBrush}"/>
        <TextBox x:Name="BlockRemove"></TextBox>
        <TextBlock Text="输入你想要移除的墙的名称." Margin="10 2" TextWrapping="Wrap" HorizontalAlignment="Left" VerticalAlignment="Stretch" Foreground="{StaticResource TextBrush}"/>
        <TextBox x:Name="WallRemove"></TextBox>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="74*"/>
                <ColumnDefinition Width="187*"/>
            </Grid.ColumnDefinitions>
            <Button Margin="5,5,0,5" Content="取消" HorizontalAlignment="Left" Padding="20, 3" VerticalAlignment="Center" Click="CancelButtonClick" Grid.ColumnSpan="2" />
            <Button Margin="0,5,5,5" Content="移除" HorizontalAlignment="Right" Padding="20, 3" VerticalAlignment="Center" Click="RemoveButtonClick" Grid.Column="1" />
        </Grid>
    </StackPanel>
</Window>

<ui:FluentWindow x:Class="TEdit.Editor.Plugins.ReplayPluginRecorderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    ResizeMode="NoResize" Width="300" Height="80"
    WindowStartupLocation="CenterOwner" Icon="/TEdit;component/Images/tedit.ico" Topmost="True"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    Title="Replay">

    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Replay Recorder"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8">
            <Button x:Name="RecordButton" Width="48" Height="36"
                    Padding="0" Margin="0,0,4,0" Click="RecordButton_Click">
                <ui:SymbolIcon x:Name="RecordIcon" Symbol="Record20" FontSize="22"/>
            </Button>
            <Button x:Name="OpenButton" Width="48" Height="36"
                    Padding="0" Margin="0,0,8,0" Click="OpenButton_Click">
                <ui:SymbolIcon Symbol="FolderOpen20" FontSize="22"/>
            </Button>
            <TextBlock x:Name="TimerText" Text="00:00:00 F:0" VerticalAlignment="Center"
                       FontFamily="Consolas" FontSize="36"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>

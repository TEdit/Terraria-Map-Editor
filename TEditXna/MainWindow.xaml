<Window x:Class="TEditXna.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:View="clr-namespace:TEditXna.View"
        Title="MainWindow" Height="350" Width="525"
    Foreground="{StaticResource TextBrush}" Background="{StaticResource WindowBackgroundBrush}">
    <Grid>
        <DockPanel LastChildFill="True">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="File">
                    <MenuItem Header="New" />
                    <MenuItem Header="Open" Command="{Binding CommandOpenWorld}" />
                    <Separator Margin="1"/>
                    <MenuItem Header="Save" />
                    <MenuItem Header="Save As" />
                    <Separator Margin="1"/>
                    <MenuItem Header="Exit" />
                </MenuItem>
                <MenuItem Header="Display">
                    <MenuItem Header="Show Backdrop" IsCheckable="True" IsChecked="True" />
                    <MenuItem Header="Show Wires" IsCheckable="True" IsChecked="False" />
                    <MenuItem Header="Show Textures" IsCheckable="True" IsChecked="False" />
                </MenuItem>
                <MenuItem Header="Help">
                    <MenuItem Header="Wiki" />
                    <Separator Margin="1" /> 
                    <MenuItem Header="Update" />
                    <MenuItem Header="Download New XML Data" />
                    <Separator Margin="1"/>
                    <MenuItem Header="About" />
                </MenuItem>
            </Menu>
            <StatusBar DockPanel.Dock="Bottom" Height="30" Background="{x:Null}" Padding="2" Foreground="{StaticResource TextBrush}">
                <StatusBarItem HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding MouseOverTile}" />
                    </StackPanel>
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Path=Progress.UserState, Mode=OneWay}" VerticalAlignment="Center" Margin="2,0" />
                        <ProgressBar Height="20" Width="100" Value="{Binding Path=Progress.ProgressPercentage, Mode=OneWay}" />
                    </StackPanel>
                </StatusBarItem>
            </StatusBar>
            <View:WorldRenderXna HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        </DockPanel>
    </Grid>
</Window>
